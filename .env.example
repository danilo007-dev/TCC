# Supabase (optional for now)
# If these are not set, the app keeps working with local in-memory data only in development.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional endpoint for AI task breakdown used in QuickCapture
VITE_AI_BREAKDOWN_ENDPOINT=
